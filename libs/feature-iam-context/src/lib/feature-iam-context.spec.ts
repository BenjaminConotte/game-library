import { featureIamContext } from './feature-iam-context';

describe('featureIamContext', () => {
  it('should work', () => {
    expect(featureIamContext()).toEqual('feature-iam-context');
  });
});
